# Phase 1: Intent Understanding & Input Analysis (เสร็จสมบูรณ์ ✅)

## เป้าหมายของเฟส
ทำให้ระบบสามารถ **เข้าใจ** ว่าผู้ใช้ต้องการอะไร โดยแยกแยะระหว่าง:
1. **คำสั่งคำนวณ** (Calculation Command) - เช่น "9.8-9.11"
2. **คำถาม** (Question) - เช่น "ต้องเตรียมเหรียญอะไรถึงจะเหมาะสม"
3. **คำสั่งทั่วไป** (General Command) - เช่น "ไปซื้อของ"

---

## 📦 สิ่งที่ถูกสร้างแล้ว

### 1. Intent Parser (`sovereign/core/intent_parser.py`)
โมดูลสำหรับวิเคราะห์ความตั้งใจของผู้ใช้ ประกอบด้วย:

#### Classes:
- `IntentType` (Enum): ประเภทของความตั้งใจ
  - `CALCULATION` - คำสั่งคำนวณ
  - `QUESTION` - คำถาม
  - `COMMAND` - คำสั่ง
  - `UNKNOWN` - ไม่ทราบ

- `ParsedIntent` (Dataclass): ผลลัพธ์การวิเคราะห์
  - `intent_type`: ประเภท
  - `original_input`: ข้อความเดิม
  - `action`: การกระทำที่ต้องการ (เช่น `calculate_-`, `suggest_payment_method`)
  - `entities`: ข้อมูลที่สกัดได้ (ตัวเลข, หน่วยเงิน)
  - `context`: บริบท
  - `confidence`: ความมั่นใจ (0.0-1.0)
  - `reasoning`: **เหตุผลในการตัดสินใจ** (แสดงวิธีคิดทีละขั้นตอน)

- `IntentParser`: ตัววิเคราะห์หลัก
  - ใช้ Regex patterns ตรวจจับรูปแบบการคำนวณ
  - ใช้ Keywords ตรวจจับคำถามและคำสั่ง
  - สร้าง reasoning steps เพื่ออธิบายการตัดสินใจ

#### ตัวอย่างการทำงาน:
```python
parser = IntentParser()
result = parser.parse("9.8-9.11")
# ผลลัพธ์:
# - intent_type: CALCULATION
# - action: calculate_-
# - entities: [9.8, 9.11]
# - reasoning: "ตรวจพบรูปแบบนิพจน์คณิตศาสตร์: 9.8 - 9.11\nไม่พบคำ/question words จึงจัดเป็น 'คำสั่งคำนวณ'"
```

---

### 2. Smart Executor (`sovereign/core/smart_executor.py`)
ตัวประมวลผลที่รวม Intent Parser กับ Logic การทำงาน

#### Features:
1. **รับ Input** → วิเคราะห์ด้วย Intent Parser
2. **แยกประเภท** → ส่งไปยัง Handler ที่เหมาะสม
   - `_handle_calculation()`: คำนวณด้วย Decimal (ความแม่นยำสูง)
   - `_handle_question()`: ตอบคำถามตาม context
   - `_handle_command()`: ดำเนินการตามคำสั่ง
3. **แสดงผล** → พร้อมรายงานการวิเคราะห์แบบละเอียด

#### ตัวอย่างผลลัพธ์:

**โจทย์ที่ 1: "9.8-9.11"**
```
✅ ผลลัพธ์: 0.69
   วิธีคิด: 9.8 - 9.11 = 0.69
```

**โจทย์ที่ 2: "แม่บอกฉันให้ไปซื้อของราคา 19 บาท..."**
```
💰 คำแนะนำการเตรียมเงินสำหรับ 19 บาท:
----------------------------------------
✅ ทางเลือกที่ 1 (จ่ายพอดี ไม่ต้องรอทอน):
   ใช้เหรียญ: 10 บาท × 1, 5 บาท × 1, 2 บาท × 2
   รวม: 3 เหรียญ

✅ ทางเลือกที่ 2 (ใช้แบงค์ 20 บาท):
   จ่าย: 20 บาท
   ได้รับทอน: 1 บาท
----------------------------------------
💡 แนะนำ: ทางเลือกที่ 1 เหมาะสมที่สุด เพราะไม่ต้องรอเงินทอน
```

---

## 🔍 กระบวนการวิเคราะห์ (Reasoning Flow)

### กรณีที่ 1: การคำนวณล้วนๆ
```
Input: "9.8-9.11"
↓
1. ตรวจพบรูปแบบนิพจน์คณิตศาสตร์: 9.8 - 9.11
2. เช็คว่ามีคำคำถามหรือไม่? → ไม่มี
3. สรุป: เป็น CALCULATION (ความมั่นใจ 99%)
4. สกัด entities: [9.8, 9.11]
5. ส่งไปยัง _handle_calculation()
6. คำนวณด้วย Decimal: 9.8 - 9.11 = 0.69
```

### กรณีที่ 2: คำถามพร้อมบริบท
```
Input: "แม่บอกฉันให้ไปซื้อของราคา 19 บาท ฉันต้องเตรียมเหรียญหรือแบงค์อะไรถึงจะเหมาะสม"
↓
1. ตรวจพบ keywords: "อะไร", "เหมาะสม" → เป็น QUESTION
2. วิเคราะห์ action: "เตรียม" + "เหรียญ/แบงค์" → suggest_payment_method
3. สกัด entities: [19, {'currency': 'THB'}]
4. สรุป: เป็น QUESTION (ความมั่นใจ 95%)
5. ส่งไปยัง _handle_question() → _suggest_payment(19)
6. คำนวณเหรียญที่เหมาะสมและแนะนำ
```

---

## ✅ การทดสอบและตรวจสอบ

### ทดสอบด้วยคำสั่ง:
```bash
cd /workspace && PYTHONPATH=/workspace python sovereign/core/smart_executor.py
```

### ผลลัพธ์ที่คาดหวัง:
| โจทย์ | ประเภท | Action | Entities | ผลลัพธ์ |
|-------|--------|--------|----------|---------|
| `9.8-9.11` | CALCULATION | `calculate_-` | `[9.8, 9.11]` | `0.69` |
| `ซื้อของ 19 บาท...` | QUESTION | `suggest_payment_method` | `[19, THB]` | `10×1, 5×1, 2×2` |

---

## 🎯 ความถูกต้องที่ตรวจสอบแล้ว

1. ✅ **การคำนวณ**: ใช้ `Decimal` แทน `float` เพื่อหลีกเลี่ยง floating-point error
   - `9.8 - 9.11 = 0.69` (ถูกต้อง)
   
2. ✅ **การแยกประเภท**: 
   - นิพจน์คณิตศาสตร์ล้วน → CALCULATION
   - มีคำคำถาม/เครื่องหมาย ? → QUESTION
   
3. ✅ **การสกัดข้อมูล**:
   - ตัวเลขทั้งหมดถูกดึงออกมา (ทั้งจำนวนเต็มและทศนิยม)
   - หน่วยเงิน (บาท) ถูกตรวจจับและเพิ่มเป็น entity
   
4. ✅ **คำแนะนำที่เหมาะสม**:
   - เรียงลำดับ banknotes จากน้อยไปมาก (20, 50, 100...) เพื่อให้ได้แบงค์ที่ใกล้เคียงที่สุด
   - สำหรับ 19 บาท → แนะนำแบงค์ 20 บาท (ทอน 1 บาท) ไม่ใช่ 1000 บาท

---

## 📋 ขั้นตอนถัดไป (Phase 2)

หลังจากเข้าใจ Input ได้ถูกต้องแล้ว เฟสถัดไปจะพัฒนา:

1. **Tool Integration**: เชื่อมต่อกับ Tool Registry ที่มีอยู่
2. **Multi-step Planning**: วางแผนหลายขั้นตอนสำหรับโจทย์ซับซ้อน
3. **Context Memory**: จดจำบริบทจากการสนทนาก่อนหน้า
4. **Language Support**: เพิ่มความสามารถภาษาไทย/อังกฤษ แบบเต็มรูปแบบ
5. **Error Handling**: จัดการกรณี Input ไม่ชัดเจนหรือผิดพลาด

---

## 📂 ไฟล์ที่เกี่ยวข้อง

```
sovereign/
├── core/
│   ├── intent_parser.py      # ✅ เสร็จแล้ว
│   └── smart_executor.py     # ✅ เสร็จแล้ว
├── engines/
│   ├── perception.py         # (ที่มีอยู่)
│   └── planner.py            # (ที่จะปรับปรุง)
└── tests/
    └── test_intent_parser.py # (จะสร้างเพิ่ม)
```
